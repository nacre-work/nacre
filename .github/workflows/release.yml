name: release

on:
  push:
    tags: ["v*"]

permissions:
  contents: read
  id-token: write        # the OIDC token npm exchanges for publish rights

jobs:
  npm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        # pnpm 10 forwards the OIDC token to the registry. pnpm 11.0.8 broke
        # that and publishes fail with a 404 (pnpm/pnpm#11513) — do not bump
        # the major here without checking that issue first.
        with: { version: 10 }
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://registry.npmjs.org
      - run: pnpm install --frozen-lockfile
      - run: pnpm build

      # Trusted publishing, no token. Each package is configured on npmjs.com
      # under Settings -> Trusted Publisher against this repository and this
      # workflow file; the registry then accepts the OIDC token above. There is
      # no NPM_TOKEN secret, so there is none to leak or rotate.
      #
      # publishConfig carries `access` but deliberately not `provenance`:
      # trusted publishing attaches provenance on its own, and the flag breaks
      # the one manual publish that has to bootstrap each package before a
      # trusted publisher can be configured for it at all.
      - run: pnpm -r --filter "./packages/*" publish --no-git-checks
