// TypeScript SDK. Contract: docs/api.md.
//
// The package is `private` until there is something in it. `files: ["dist"]`
// with an empty barrel publishes a working install of nothing, and the first
// `v*` tag would have done exactly that — taking the name on the registry with
// a package that exports `{}`.
// Nothing is implemented yet. Order of work is fixed by docs/authz.md — the
// permission model and its tests land before any retrieval or client code.
export {}
