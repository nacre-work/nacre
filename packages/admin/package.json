{
  "name": "@nacre.work/admin",
  "version": "0.12.1",
  "description": "Community admin UI — single organization",
  "type": "module",
  "scripts": {
    "build": "tsc -b && node ./scripts/build.mjs",
    "dev": "node ./scripts/build.mjs --watch"
  },
  "private": true,
  "dependencies": {
    "@nacre.work/sdk": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "jsqr": "1.4.0"
  }
}
