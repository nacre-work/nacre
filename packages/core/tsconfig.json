{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "rootDir": ".", "outDir": "dist" },
  "include": ["**/*.ts"],
  "comment": "test-plan.ts is the T1-T15 inventory, not library API — it is read by the coverage test and by scripts/authz-pending.ts, both from source, and has no business in a published package.",
  "exclude": ["dist", "**/__tests__/**", "authz/test-plan.ts"]
}
